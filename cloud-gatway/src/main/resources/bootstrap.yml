spring:
  application:
    name: cloud-gatway
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      fail-fast: true


eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka

